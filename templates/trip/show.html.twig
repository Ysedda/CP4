{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}
	| Trajet -
	{{ trip.meetingPoint }}
{% endblock %}

{% block body %}
	<section class="home-banner min-vh-100">
		<div class="container">
			<div class="row">
				<div class="d-flex align-items-center col-md-8 col-12 justify-content-center container">
					<div class="bg-light col-lg-6 col-11 rounded-3 shadow p-4 mt-5">
						<div class="d-flex w-100 justify-content-between">
							<a class="btn btn-dark" href="{{ path('app_trip_index') }}">
								<i class="fa-regular fa-circle-left"></i>
								Retour</a>
							{% include 'trip/_accept_trip.html.twig' %}
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>
	{{ include('trip/_delete_form.html.twig') }}
{% endblock %}
